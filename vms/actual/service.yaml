apiVersion: v1
kind: Service
metadata:
  name: actual
  namespace: vms
  labels:
    app.kubernetes.io/name: actual
    app.kubernetes.io/part-of: kubevirt-migration
spec:
  type: ClusterIP
  selector:
    vm.kubevirt.io/name: actual
  ports:
    - name: http
      port: 11051
      protocol: TCP
      targetPort: 11051
