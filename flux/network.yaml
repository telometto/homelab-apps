apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: network
  namespace: flux-system
spec:
  interval: 10m
  path: ./network
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
