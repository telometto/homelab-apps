apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: cdi-config
  namespace: flux-system
spec:
  interval: 10m
  path: ./cdi/config
  prune: false
  sourceRef:
    kind: GitRepository
    name: flux-system
  dependsOn:
    - name: cdi
  timeout: 10m
  wait: true
